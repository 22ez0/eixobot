services:
  - type: background
    name: discord-bot
    env: node
    region: oregon
    buildCommand: npm run render-build
    startCommand: npm run render-start
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: CLIENT_ID  
        sync: false
      - key: NODE_ENV
        value: production